<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="148dp"
    android:layout_height="220dp"
    android:layout_margin="4dp"
    app:cardCornerRadius="16dp">

    <ImageView
        android:id="@+id/imgFeaturedPhoto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/featured_image"
        android:scaleType="centerCrop" />

    <ProgressBar
        android:id="@+id/progressBarLoading"
        android:layout_width="@dimen/progress_size"
        android:layout_height="@dimen/progress_size"
        android:layout_gravity="center" />

    <ImageButton
        android:layout_width="@dimen/small_image_button_size"
        android:layout_height="@dimen/small_image_button_size"
        android:layout_gravity="end"
        android:layout_margin="12dp"
        android:background="@drawable/bookmark_button_back"
        android:contentDescription="@string/featured_bookmark"
        android:src="@drawable/ic_bookmark" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_margin="16dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvFeaturedCountryName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/core_sans_c_55"
            android:letterSpacing="-0.03"
            android:lineSpacingExtra="6sp"
            android:textColor="@color/white" />

        <TextView
            android:id="@+id/tvFeaturedCityName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/core_sans_c_55"
            android:letterSpacing="-0.03"
            android:lineSpacingExtra="6sp"
            android:textColor="@color/white" />
    </LinearLayout>

</androidx.cardview.widget.CardView>